import 'package:flutter/material.dart';

class NetOverallScreen extends StatelessWidget {
  const NetOverallScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(
        children: [
          Center(
            child: Text("Net Overall"),
          ),
        ],
      ),
    );
  }
}
